import React from 'react';
const Filtro = () => {
	// ordenar por precio
	// productos.sort((a, b) => a.precio - b.precio);

	//filtrar por rango de precios.
	// let productosa = productos.filter(
	// 	(producto) =>
	// 		precioBase <= producto.precio && producto.precio <= precioTope
	// );

	//filtrar por marca
	// let productosMarca = productos.filter(
	// 	(producto) => producto.marca == marca
	// );

	return (
		<>
			<h1>filtro</h1>
		</>
	);
};

export default Filtro;
